const baseUrl = 'http://api.liftron.com.ua';
// const baseUrl = 'http://rest-api-simple.local';

export const urls = {
  vacancy: {
    base: `${baseUrl}/vacancy`,
  },
  filter: {
    all: `${baseUrl}/filter`,
  }
};